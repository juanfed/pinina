import makeStyles from '@mui/styles/makeStyles';

export const imageLoaderStyles = makeStyles((theme) => ({
	img: {
		borderRadius: '100px',
		borderStyle: 'solid',
		borderColor: '#6F502C'
	}
}));

export default imageLoaderStyles;
